---
type: template
title: Animate an image (custom-video-producer-animate-an-image)
slug: custom-video-producer-animate-an-image
created: 2026-08-29T17:10:46.738Z
templateId: custom-video-producer-animate-an-image
role: Custom
originTask: Using the supplied image as the first frame, write a motion prompt describing how the scene should move, then use media.video with that image as first_frame_image to generate a short clip. If no image was supplied, ask for one. Return the prompt + the video URL.
---

# Animate an image (custom-video-producer-animate-an-image)

Take a supplied image as the first frame and generate a short motion clip from it.

**Origin task:** Using the supplied image as the first frame, write a motion prompt describing how the scene should move, then use media.video with that image as first_frame_image to generate a short clip. If no image was supplied, ask for one. Return the prompt + the video URL.

## Saved plan

```json
{
  "steps": [],
  "waves": []
}
```
